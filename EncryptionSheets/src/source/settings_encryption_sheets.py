import os

from settings import ROOT_DIR

# Имя выходного файла.
DEFAULT_FILENAME = 'Шифровки'

# Путь к файлу с дизайном интерфейса.
PATH_UI = os.path.join(ROOT_DIR, 'EncryptionSheets/src/ui/main_window.ui')
